name: portix
version: "1.0.0"
summary: Portix SSH Client - Multi-platform SSH terminal with SFTP
description: |
  Portix is a modern SSH client built with Flutter UI and Rust SSH core.
  Features include multi-tab terminal sessions, split workspace, SFTP file
  manager, remote file browsing, command autocomplete, and cross-platform
  support for Linux, macOS, and Windows.

icon: portix_app/assets/icons/portix_launcher.png

base: core22
grade: stable
confinement: strict

architectures:
  - build-on: amd64

parts:
  portix:
    plugin: nil
    source: .
    source-type: local
    build-packages:
      - nothing # Placeholder
    stage-packages:
      - libgtk-3-0
      - libsecret-1-0
      - libgcrypt20
    override-build: |
      # Copy pre-built bundle ke install dir
      # Bundle sudah ada di /tmp/artifact/bundle/ dari workflow
      if [ -d /tmp/artifact/bundle ]; then
        cp -r /tmp/artifact/bundle/. $CRAFT_PART_INSTALL/
      else
        echo "ERROR: Pre-built bundle not found"
        exit 1
      fi

apps:
  portix:
    command: portix
    desktop: usr/share/applications/portix.desktop
    extensions: [gnome]
    plugs:
      - network
      - network-bind
      - home
      - desktop
      - desktop-legacy
      - wayland
      - x11
      - opengl
      - gsettings
      - password-manager-service
      - ssh-keys

layout:
  /usr/share/libdrm:
    bind: $SNAP/usr/share/libdrm
